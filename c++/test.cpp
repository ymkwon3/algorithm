#include <iostream>
#include <vector>
#include <map>
#include <algorithm>
#include <typeinfo>
#include <queue>

using namespace std;
map<int, vector<int>> m;
vector<int> v;
vector<int>::iterator iter = v.begin();
int g[100] = {
    -1,
};
int main(void)
{

    cout << g[5] << "\n";
    return 0;
}